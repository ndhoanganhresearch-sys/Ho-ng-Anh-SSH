# SSL Smart Tunnel Monitoring System (4D-LiDAR) - pinned dependencies
# Python 3.12. Install: pip install -r requirements.txt

# Core numerics
numpy==2.4.3
scipy==1.17.1

# Point-cloud I/O and processing
laspy==2.7.0
lazrs==0.8.1
open3d==0.19.0

# Registration (parallel GICP/ICP)
small_gicp==1.0.0

# 4D change analysis (M3C2)
py4dgeo==0.7.0

# GUI + 3D visualisation
PySide6==6.11.0
pyvista==0.48.0
pyvistaqt==0.11.4
vtk==9.3.1

# Plotting / reporting
matplotlib==3.10.8
pandas==3.0.3
seaborn==0.13.2

# Reporting / BIM (used by exporter.py, ifc_exporter.py, TunnelApp.py)
openpyxl==3.1.5
ifcopenshell==0.8.5

# Machine learning clustering (target_detector.py, preprocessing.py DBSCAN)
scikit-learn==1.8.0

# Local AI assistant / dashboards (rag_ai.py, digital_twin.py)
requests==2.32.5
# Optional context compression for AI prompts (headroom_adapter.py).
# Full Headroom native compression is installed in WSL via .venv-headroom:
#   pip install "headroom-ai[mcp,proxy,code]==0.23.0"
# Windows builds may be blocked by local Application Control, so the adapter
# falls back safely when headroom._core is unavailable.

# Optional OCR / document parsing for reports, standards, and inspection images (ocr_adapter.py).
# Install basic OCR first; add extras such as paddleocr[doc-parser] only when needed.
# PaddleOCR 3.6.0 + PaddlePaddle 3.3.x hit a Windows CPU oneDNN issue in testing:
#   pip install paddleocr==3.4.0 paddlepaddle==3.2.1
